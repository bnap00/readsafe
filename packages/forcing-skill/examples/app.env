# Example environment for the ReadSafe forcing-skill package.
# These are illustrative placeholders, not real credentials. They exist so the
# documented commands in SKILL.md are runnable and verified in CI.
APP_NAME=example-service
API_PORT=8080
DATABASE_URL=postgres://user:example-password@localhost:5432/app
API_TOKEN=example-token-not-a-real-secret  # rotate quarterly
SUPPORT_EMAIL=support@example.com
